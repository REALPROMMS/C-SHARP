Console.Write("Введите любое число: ");
int n = int.Parse(Console.ReadLine());
int i = 1;
double sum = 0;
for ( i = 1; i <= n; i++)
	Console.WriteLine(sum = Math.Pow(i, 2));


// Вывод число от 1 до N в квадрат